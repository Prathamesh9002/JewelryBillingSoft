<Window x:Class="JewelryBillingSoft.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Jewelry Billing Soft - Login"
        Height="500" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border CornerRadius="16" ClipToBounds="True">
        <Grid>
            <!-- Background gradient -->
            <Border>
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#1A1A2E" Offset="0"/>
                        <GradientStop Color="#16213E" Offset="0.5"/>
                        <GradientStop Color="#0F3460" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <Grid Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,30">
                    <!-- Logo placeholder -->
                    <Border Width="70" Height="70" CornerRadius="35" HorizontalAlignment="Center" Margin="0,0,0,15">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#B8860B" Offset="0"/>
                                <GradientStop Color="#FFD700" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="💎" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Jewelry Billing Soft" FontSize="22" FontWeight="Bold"
                               Foreground="#FFD700" HorizontalAlignment="Center"/>
                    <TextBlock Text="Sign in to continue" FontSize="12"
                               Foreground="#AAAAAA" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Form -->
                <StackPanel Grid.Row="2" VerticalAlignment="Center">
                    <!-- Error Message -->
                    <Border Background="#FF4444" CornerRadius="6" Padding="12,8" Margin="0,0,0,15"
                            Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="White" FontSize="12"/>
                    </Border>

                    <!-- Username -->
                    <TextBlock Text="Username" Foreground="#AAAAAA" FontSize="12" Margin="0,0,0,5"/>
                    <Border Background="#FFFFFF15" CornerRadius="8" BorderBrush="#FFFFFF30" BorderThickness="1" Margin="0,0,0,15">
                        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                                 Background="Transparent" BorderThickness="0"
                                 Foreground="White" CaretBrush="White"
                                 FontSize="14" Padding="12,10"/>
                    </Border>

                    <!-- Password -->
                    <TextBlock Text="Password" Foreground="#AAAAAA" FontSize="12" Margin="0,0,0,5"/>
                    <Border Background="#FFFFFF15" CornerRadius="8" BorderBrush="#FFFFFF30" BorderThickness="1" Margin="0,0,0,25">
                        <PasswordBox x:Name="PasswordBox" Background="Transparent" BorderThickness="0"
                                     Foreground="White" CaretBrush="White"
                                     FontSize="14" Padding="12,10"
                                     PasswordChanged="PasswordBox_PasswordChanged"/>
                    </Border>

                    <!-- Login Button -->
                    <Button Command="{Binding LoginCommand}"
                            Height="46" FontSize="15" FontWeight="SemiBold"
                            Cursor="Hand" BorderThickness="0">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#B8860B" Offset="0"/>
                                <GradientStop Color="#FFD700" Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock Text="Sign In" Foreground="White" FontSize="15" FontWeight="Bold"/>
                    </Button>

                    <!-- Loading indicator -->
                    <ProgressBar IsIndeterminate="True" Height="3" Margin="0,10,0,0"
                                 Foreground="#FFD700" Background="Transparent"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>

                <!-- Footer -->
                <TextBlock Grid.Row="3" Text="Default: admin / Admin@123"
                           Foreground="#666666" FontSize="11" HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
            </Grid>

            <!-- Close button -->
            <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,10,10,0" Width="30" Height="30"
                    Click="CloseButton_Click"
                    Background="Transparent" Foreground="#AAAAAA" BorderThickness="0"
                    FontSize="14" Cursor="Hand"/>
        </Grid>
    </Border>
</Window>

