<UserControl x:Class="JewelryBillingSoft.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="20">
        <StackPanel>
            <TextBlock Text="Settings" FontSize="26" FontWeight="Bold" Foreground="#1A1A2E" Margin="0,0,0,20"/>

            <!-- Shop Details -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🏪 Shop Details" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,15,0">
                            <TextBlock Text="Shop Name" FontSize="12" Foreground="#666666"/>
                            <TextBox Text="{Binding ShopName}" Margin="0,4,0,12"/>

                            <TextBlock Text="Mobile" FontSize="12" Foreground="#666666"/>
                            <TextBox Text="{Binding Mobile}" Margin="0,4,0,12"/>

                            <TextBlock Text="GST Number" FontSize="12" Foreground="#666666"/>
                            <TextBox Text="{Binding GstNumber}" Margin="0,4,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Address" FontSize="12" Foreground="#666666"/>
                            <TextBox Text="{Binding ShopAddress}" Height="70" AcceptsReturn="True" Margin="0,4,0,12"/>

                            <TextBlock Text="Email" FontSize="12" Foreground="#666666"/>
                            <TextBox Text="{Binding Email}" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>

                    <Button Content="💾 Save Settings" Command="{Binding SaveSettingsCommand}"
                            Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left" Margin="0,20,0,0"/>
                </StackPanel>
            </Border>

            <!-- Change Password -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🔑 Change Password" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>
                    <Grid MaxWidth="400" HorizontalAlignment="Left">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Current Password" FontSize="12" Foreground="#666666"/>
                        <PasswordBox Grid.Row="0" Margin="0,22,0,12"/>
                        <TextBlock Grid.Row="1" Text="New Password" FontSize="12" Foreground="#666666"/>
                        <PasswordBox Grid.Row="1" Margin="0,22,0,12"/>
                        <TextBlock Grid.Row="2" Text="Confirm Password" FontSize="12" Foreground="#666666"/>
                        <PasswordBox Grid.Row="2" Margin="0,22,0,0"/>
                    </Grid>
                    <Button Content="🔒 Change Password" Command="{Binding ChangePasswordCommand}"
                            Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left" Margin="0,15,0,0"/>
                </StackPanel>
            </Border>

            <!-- About -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="ℹ️ About" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock Text="Jewelry Billing Soft v1.0.0" FontSize="14" Margin="0,0,0,5"/>
                    <TextBlock Text="A complete billing solution for jewelry shops" Foreground="#888888" FontSize="12"/>
                    <TextBlock Text="© 2024 JewelrySoft. All rights reserved." Foreground="#888888" FontSize="12" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>

